// lines starting with // are comments - the computer ignores them
// other lines are commands that the computer runs

var rood = 20
var g = 255
var b = 20
var t = 50

function setup() {
  createCanvas(400, 100)

  background(g, rood, b, t)
}
